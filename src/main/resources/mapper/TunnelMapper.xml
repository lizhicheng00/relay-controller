<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.huawei.devbridge.relaycontroller.infrastructure.persistence.mapper.TunnelMapper">
    <select id="selectByTunnelIdAndRegion"
            resultType="com.huawei.devbridge.relaycontroller.infrastructure.persistence.entity.TunnelEntity">
        SELECT t.*
        FROM tunnel t
        INNER JOIN cluster c ON c.cluster_id = t.cluster_id
        WHERE t.tunnel_id = #{tunnelId}
          AND c.region = #{region}
          AND t.deleted = 0
        LIMIT 1
    </select>

    <select id="selectByTunnelIdAndRegionForUpdate"
            resultType="com.huawei.devbridge.relaycontroller.infrastructure.persistence.entity.TunnelEntity">
        SELECT t.*
        FROM tunnel t
        INNER JOIN cluster c ON c.cluster_id = t.cluster_id
        WHERE t.tunnel_id = #{tunnelId}
          AND c.region = #{region}
          AND t.deleted = 0
        LIMIT 1
        FOR UPDATE
    </select>

    <select id="selectByTunnelIdsAndRegion"
            resultType="com.huawei.devbridge.relaycontroller.infrastructure.persistence.entity.TunnelEntity">
        SELECT t.*
        FROM tunnel t
        INNER JOIN cluster c ON c.cluster_id = t.cluster_id
        WHERE c.region = #{region}
          AND t.deleted = 0
          AND t.tunnel_id IN
        <foreach collection="tunnelIds" item="tunnelId" open="(" separator="," close=")">
            #{tunnelId}
        </foreach>
    </select>

    <select id="selectByNamespaceAndRegion"
            resultType="com.huawei.devbridge.relaycontroller.infrastructure.persistence.entity.TunnelEntity">
        SELECT t.*
        FROM tunnel t
        INNER JOIN cluster c ON c.cluster_id = t.cluster_id
        WHERE t.namespace = #{namespace}
          AND c.region = #{region}
          AND t.deleted = 0
        ORDER BY t.created_at DESC
    </select>

    <select id="selectActiveByNamespaceAndRegion"
            resultType="com.huawei.devbridge.relaycontroller.infrastructure.persistence.entity.TunnelEntity">
        SELECT t.*,
               (SELECT COUNT(1) FROM tunnel_port tp WHERE tp.tunnel_code = t.tunnel_code) AS port_count
        FROM tunnel t
        INNER JOIN cluster c ON c.cluster_id = t.cluster_id
        WHERE t.namespace = #{namespace}
          AND c.region = #{region}
          AND t.deleted = 0
          AND t.expiration &gt; #{now}
        <if test="clusterId != null and clusterId != ''">
          AND t.cluster_id = #{clusterId}
        </if>
        ORDER BY t.created_at DESC
    </select>

    <select id="countActiveByAccountId" resultType="long">
        SELECT COUNT(1)
        FROM tunnel
        WHERE account_id = #{accountId}
          AND deleted = 0
          AND expiration &gt; #{now}
    </select>

    <select id="selectAgedByRegion"
            resultType="com.huawei.devbridge.relaycontroller.infrastructure.persistence.entity.TunnelEntity">
        SELECT t.*
        FROM tunnel t
        INNER JOIN cluster c ON c.cluster_id = t.cluster_id
        WHERE c.region = #{region}
          AND t.expiration &lt;= #{expirationCutoff}
        ORDER BY t.expiration ASC
        LIMIT #{limit}
    </select>

    <update id="increaseBandwidthUsed">
        UPDATE tunnel t
        INNER JOIN cluster c ON c.cluster_id = t.cluster_id
        SET t.bandwidth_used = t.bandwidth_used + #{usageBytes},
            t.updated_at = #{updatedAt}
        WHERE t.tunnel_id = #{tunnelId}
          AND c.region = #{region}
          AND t.deleted = 0
    </update>

    <update id="refreshExpiration">
        UPDATE tunnel t
        INNER JOIN cluster c ON c.cluster_id = t.cluster_id
        SET t.expiration = GREATEST(t.expiration, #{activityAt} + t.expiration_hours * 3600)
        WHERE t.tunnel_id = #{tunnelId}
          AND c.region = #{region}
          AND t.deleted = 0
          AND t.expiration &gt; #{activityAt}
    </update>

    <update id="refreshExpirationFromHeartbeat">
        UPDATE tunnel t
        INNER JOIN cluster c ON c.cluster_id = t.cluster_id
        SET t.expiration = #{activityAt} + t.expiration_hours * 3600
        WHERE t.tunnel_id = #{tunnelId}
          AND c.region = #{region}
          AND t.deleted = 0
          AND t.expiration &gt; #{activityAt}
          AND t.expiration &lt; #{activityAt} + t.expiration_hours * 3600 - #{minimumExtensionSeconds}
    </update>
</mapper>
